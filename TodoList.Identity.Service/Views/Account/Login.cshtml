@model TodoList.Identity.Service.Models.LoginViewModel

@{
    ViewData["Title"] = "Login";
}

<div class="text-center">
    <h1>Single Sign-On</h1>
</div>


<div class="row m-1">
    <div class="offset-4 col-4">
        <div class="border border-2 p-2 rounded border-light">
            <form method="post" asp-controller="Account" asp-action="Login">
                @Html.AntiForgeryToken()

                <partial name="_ValidationSummary" />

                <div class="mb-3">
                    <label asp-for="@Model.Username" class="form-label"></label>
                    <input type="text" asp-for="@Model.Username" class="form-control" placeholder="name@example.com" />
                    <span asp-validation-for="@Model.Username" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="@Model.Password" class="form-label"></label>
                    <input type="password" asp-for="@Model.Password" class="form-control" />
                    <span asp-validation-for="@Model.Password" class="text-danger"></span>
                </div>
                <div>
                    <input type="hidden" asp-for="@Model.ReturnUrl" />
                    <button type="submit" class="btn btn-primary">Login</button>
                    <a asp-action="Register" asp-controller="Account" asp-route-returnUrl="@Model.ReturnUrl" asp- class="btn btn-secondary">Register</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
